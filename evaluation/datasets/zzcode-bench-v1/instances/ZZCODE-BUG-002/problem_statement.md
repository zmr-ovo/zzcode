# `.env` 文件可能通过工作区上下文和工具被读取

工作区中可能存在包含密钥的 `.env` 文件。目前文件列表/上下文没有统一排除它，Agent 还可能通过 `read_file` 或指定路径的 `search` 直接访问它。

请修改实现：

- `.env` 不得出现在 Agent 可浏览的工作区文件列表中。
- `read_file` 不得读取 `.env`。
- `search` 不得以 `.env` 或其内部路径作为搜索范围。
- 规则应由统一的私密路径定义驱动，避免工作区扫描与工具校验产生不同策略。
- 保留普通文件的读取、搜索和现有忽略规则。
