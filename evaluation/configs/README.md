# Evaluation configuration

Provider, model, resource-limit, environment, and release-gate configuration
will be added when execution is implemented. Phase 1 deliberately keeps those
settings out of the dataset schema and does not commit placeholder credentials.
